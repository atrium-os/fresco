// Autonomous GPU tasks — these run every frame without CPU involvement
// Each module implements a task type that the KarythraGPU runs autonomously

// Phase 1: stubs — task infrastructure only
// Phase 2: physics, particles, animation (Metal compute shaders)
// Phase 3: GI probes, audio raytracing (Metal ray tracing API)
